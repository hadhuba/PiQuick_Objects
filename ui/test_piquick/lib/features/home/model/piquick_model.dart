class PiquickModel {
  String currentObj;
  String? currentAnimation;
  String? currentTexture;

  PiquickModel({
    required this.currentObj,
    this.currentAnimation,
    this.currentTexture,
  });
}